import CommerceApp from "@/components/commerce-app";

export const metadata = {
  title: "Favorites | Eona Empire",
};

export default function FavoritesPage() {
  return <CommerceApp view="favorites" />;
}
