import CommerceApp from "@/components/commerce-app";

export const metadata = {
  title: "Shopping Cart | Eona Empire",
};

export default function CartPage() {
  return <CommerceApp view="cart" />;
}
