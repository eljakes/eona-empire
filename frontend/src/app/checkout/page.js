import CommerceApp from "@/components/commerce-app";

export const metadata = {
  title: "Checkout | Eona Empire",
};

export default function CheckoutPage() {
  return <CommerceApp view="checkout" />;
}
