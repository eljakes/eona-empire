import CommerceApp from "@/components/commerce-app";

export const metadata = {
  title: "Admin Overview | Eona Empire",
};

export default function AdminPage() {
  return <CommerceApp view="admin" />;
}
